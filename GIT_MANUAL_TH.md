# คู่มือการใช้ Git (ฉบับมือใหม่ & สำหรับโปรเจคนี้) 🇹🇭

Git คือเครื่องมือช่วย "บันทึกประวัติ" การแก้ไขโค้ดของเรา ทำให้เราสามารถย้อนกลับไปดูเวอร์ชันเก่าๆ ได้ และช่วยให้ทำงานร่วมกับคนอื่นได้ง่ายขึ้น (ผ่าน GitHub/GitLab)

---

## 1. เริ่มต้นใช้งาน (First Time Setup)

ถ้าเพิ่งเริ่มใช้ครั้งแรกในเครื่อง ต้องตั้งชื่อและอีเมลก่อน (ทำแค่ครั้งเดียว):

```bash
git config --global user.name "ชื่อของคุณ"
git config --global user.email "email@example.com"
```

---

## 2. เริ่มต้นโปรเจค (Init)

เปิด Terminal ที่โฟลเดอร์โปรเจค (`aideelHadist`) แล้วพิมพ์:

```bash
git init
```
*(คำสั่งนี้จะสร้างโฟลเดอร์ลับ `.git` ขึ้นมาเพื่อเริ่มเก็บข้อมูล)*

---

## 3. การบันทึกงาน (Save / Commit)

การบันทึกใน Git มี 2 จังหวะ: **เลือกไฟล์ (Add)** -> **บันทึก (Commit)**

### 3.1 เลือกไฟล์ที่จะบันทึก
```bash
# เลือกทุกไฟล์ที่มีการเปลี่ยนแปลง
git add .
```

### 3.2 บันทึกพร้อมข้อความกันลืม
```bash
git commit -m "ข้อความอธิบายว่าแก้อะไรไป เช่น แก้ไขตัวเลขสถิติ Ahmad"
```

---

## 4. ดูสถานะ (Check Status)

เช็คว่ามีไฟล์ไหนแก้ไปแล้วบ้าง หรือไฟล์ไหนยังไม่ได้ Add:

```bash
git status
```

*   **สีแดง:** ไฟล์มีการแก้ไข แต่ยังไม่ได้ `git add`
*   **สีเขียว:** ไฟล์พร้อมที่จะ `git commit`

---

## 5. การอัปขึ้น Cloud (GitHub / GitLab)

เพื่อให้โค้ดเราปลอดภัย หายห่วง หรือเอาไป Deploy บน Server ได้ง่ายขึ้น

1.  ไปสร้าง Repository ใหม่ที่ GitHub.com (New Repository)
2.  ก๊อปปี้ URL มา (เช่น `https://github.com/username/my-project.git`)
3.  กลับมาที่ Terminal พิมพ์:

```bash
# เชื่อมต่อกับ GitHub (ทำครั้งแรกครั้งเดียว)
git remote add origin https://github.com/username/my-project.git

# เปลี่ยนชื่อ branch หลักเป็น main (มาตรฐานใหม่)
git branch -M main

# อัปโหลดโค้ดขึ้นไป
git push -u origin main
```

---

## 6. คำสั่งที่ใช้บ่อยในชีวิตประจำวัน (Daily Workflow) 🔄

ทุกครั้งที่แก้โค้ดเสร็จและ Test ผ่านแล้ว ให้ทำสูตรนี้:

1.  `git status` (เช็คความเรียบร้อย)
2.  `git add .` (เลือกทั้งหมด)
3.  `git commit -m "ทำอะไรไป..."` (บันทึก)
4.  `git push` (ส่งขึ้น Cloud)

---

## 💡 Tips สำหรับโปรเจคนี้

### ไฟล์ `.gitignore`
สำคัญมาก! เราต้องไม่เอาไฟล์ขยะ หรือไฟล์ขนาดใหญ่ขึ้น Git (เช่น `node_modules`, `.env`)
ตรวจสอบว่าใน root folder มีไฟล์ชื่อ `.gitignore` และมีเนื้อหาประมาณนี้:

```text
node_modules/
dist/
.env
.DS_Store
__pycache__/
deployment_package/
```

### การ Deploy ด้วย Git (ทางเลือกแทน Zip)
ถ้าเราเอาโค้ดขึ้น GitHub แล้ว เวลาจะ Deploy บน Server สามารถทำแบบนี้ได้:

1.  **บน Server:** `git pull` (ดึงโค้ดล่าสุดลงมา)
2.  **บน Server:** `npm install` (ถ้ามี lib ใหม่)
3.  **บน Server:** `npm run build`
4.  **บน Server:** `pm2 restart all`

(วิธีนี้จะเร็วกว่าการ Zip Upload ขึ้นไปใหม่ทุกครั้งมากครับ)

---
const menuItems = [
    { name: "ภาพรวม", href: "/admin", icon: "📊" },
    { name: "จัดการหนังสือ", href: "/admin/books", icon: "📚" },
    { name: "จัดการกิตาบ", href: "/admin/kitabs", icon: "📁" },
    { name: "จัดการหะดีษ", href: "/admin/hadiths", icon: "📖" },
    { name: "หมวดหมู่บทความ", href: "/admin/categories", icon: "🏷️" },
    { name: "บทความ (Articles)", href: "/admin/articles", icon: "📝" },
    { name: "สถานะการแปล", href: "/admin/translations", icon: "🌍" },
];

const { pathname } = Astro.url;
---

<aside
    class="w-64 bg-white border-r border-gray-200 min-h-screen hidden md:block"
>
    <div class="p-6">
        <a href="/admin" class="flex items-center gap-3">
            <div
                class="w-10 h-10 bg-primary/10 rounded-xl flex items-center justify-center text-primary"
            >
                🛡️
            </div>
            <span class="text-xl font-bold text-primary">Admin Panel</span>
        </a>
    </div>

    <nav class="px-4 py-2 space-y-1">
        {
            menuItems.map((item) => {
                const isActive =
                    pathname === item.href ||
                    (item.href !== "/admin" && pathname.startsWith(item.href));
                return (
                    <a
                        href={item.href}
                        class={`flex items-center gap-3 px-4 py-3 rounded-xl transition-all font-medium ${
                            isActive
                                ? "bg-primary text-white shadow-md shadow-primary/20"
                                : "text-gray-600 hover:bg-gray-50 hover:text-primary"
                        }`}
                    >
                        <span class="text-xl">{item.icon}</span>
                        {item.name}
                    </a>
                );
            })
        }
    </nav>

    <div class="absolute bottom-0 w-64 p-4 border-t border-gray-100">
        <a
            href="/"
            class="flex items-center gap-2 px-4 py-2 text-sm text-gray-500 hover:text-primary transition-colors"
        >
            <span>←</span> กลับหน้าหลัก
        </a>
        <button
            id="logout-btn"
            class="mt-2 w-full flex items-center justify-center gap-2 px-4 py-2 border border-red-200 text-red-600 rounded-lg hover:bg-red-50 transition-colors text-sm font-medium"
        >
            🚪 ออกจากระบบ
        </button>
    </div>
</aside>

<script>
    document.getElementById("logout-btn")?.addEventListener("click", () => {
        document.cookie = "admin_key=; path=/; max-age=0";
        window.location.href = "/admin/login";
    });
</script>

---
interface Props {
    title: string;
}

const { title } = Astro.props;

// Auth Check
const cookie = Astro.cookies.get("admin_key");
const validKey = import.meta.env.ADMIN_SECRET || "bismillah";

if (!cookie || cookie.value !== validKey) {
    return Astro.redirect("/admin/login");
}

const currentPath = Astro.url.pathname;

const menuItems = [
    { name: "ภาพรวม", href: "/admin", icon: "📊" },
    { name: "จัดการหนังสือ", href: "/admin/books", icon: "📚" },
    { name: "จัดการบท (Kitabs)", href: "/admin/kitabs", icon: "📑" },
    { name: "จัดการหะดีษ", href: "/admin/hadiths", icon: "📝" },
    { name: "จัดการบทความ", href: "/admin/articles", icon: "📰" },
    { name: "จัดการหมวดหมู่", href: "/admin/categories", icon: "🏷️" },
];
---

<!doctype html>
<html lang="th">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{title} | Admin Panel</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Thai+Looped:wght@300;400;500;600;700&family=Amiri:wght@400;700&display=swap"
            rel="stylesheet"
        />
        <script src="https://cdn.tailwindcss.com"></script>
        <style>
            body {
                font-family: "IBM Plex Sans Thai Looped", sans-serif;
            }
            .font-arabic {
                font-family: "Amiri", serif;
            }
        </style>
    </head>
    <body class="bg-gray-50 text-gray-800">
        <div class="flex h-screen overflow-hidden">
            <!-- Sidebar -->
            <aside
                class="hidden w-64 overflow-y-auto border-r border-gray-200 bg-white md:block flex-shrink-0"
            >
                <div
                    class="flex h-16 items-center justify-center border-b border-gray-200 px-6"
                >
                    <span class="text-2xl font-bold text-primary">🛡️ Admin</span
                    >
                </div>
                <div class="px-4 py-6">
                    <nav class="space-y-1">
                        {
                            menuItems.map((item) => (
                                <a
                                    href={item.href}
                                    class={`flex items-center rounded-lg px-4 py-3 text-sm font-medium transition-colors ${
                                        currentPath === item.href
                                            ? "bg-primary/10 text-primary"
                                            : "text-gray-700 hover:bg-gray-50 hover:text-primary"
                                    }`}
                                >
                                    <span class="mr-3 text-xl">
                                        {item.icon}
                                    </span>
                                    {item.name}
                                </a>
                            ))
                        }
                    </nav>
                </div>
                <div
                    class="absolute bottom-0 w-full border-t border-gray-200 p-4"
                >
                    <a
                        href="/"
                        target="_blank"
                        class="flex items-center justify-center rounded-lg border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50"
                    >
                        ไปหน้าเว็บไซต์ ↗
                    </a>
                </div>
            </aside>

            <!-- Main Content -->
            <div class="flex flex-1 flex-col overflow-hidden">
                <!-- Top Header (Mobile Only mostly) -->
                <header
                    class="flex h-16 items-center justify-between border-b border-gray-200 bg-white px-6 md:hidden"
                >
                    <div class="font-bold text-lg">Admin Panel</div>
                    <button class="text-gray-500 focus:outline-none md:hidden">
                        <!-- Mobile menu button placeholder -->
                        ☰
                    </button>
                </header>

                <header
                    class="hidden h-16 items-center justify-between border-b border-gray-200 bg-white px-8 md:flex"
                >
                    <h1 class="text-xl font-semibold text-gray-800">{title}</h1>
                    <div class="flex items-center gap-4">
                        <span class="text-sm text-gray-500"
                            >Logged in as Admin</span
                        >
                        <div
                            class="h-8 w-8 rounded-full bg-primary/20 flex items-center justify-center text-primary"
                        >
                            A
                        </div>
                    </div>
                </header>

                <!-- Content Scroll Area -->
                <main class="flex-1 overflow-y-auto bg-gray-50 p-6 md:p-8">
                    <slot />
                </main>
            </div>
        </div>
    </body>
</html>

<script>
    // Simple config needed for Tailwind CDN if not using build properly
    // But we are using astro tailwind integration so it should handle classes.
    // The CDN link above is redundant if integration works, but safe fallback.
    // Actually, conflicting if integration is active.
    // Let's remove CDN link in favor of integration if it works, BUT
    // Since I'm not sure if tailwind integration is perfectly set up for this layout
    // (it should be since it's in astro config), I'll rely on integration.
    // Wait, the previous files didn't have CDN.
    // I will remove the CDN line to check.
</script>
